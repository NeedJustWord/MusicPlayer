<controls:BaseWindow x:Class="MusicPlayer.Windows.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:MusicPlayer.Controls"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        MouseUp="BaseWindow_MouseUp"
        Style="{StaticResource AboutWindowStyle}">
    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBlock Text="本软件使用WPF技术，模仿多米音乐制作而成" Style="{StaticResource DescriptionStyle}"/>
            <TextBlock Text="仅供技术学习使用，不可用于商业用途" Style="{StaticResource DescriptionStyle}"/>
            <TextBlock/>
            <TextBlock Text="作者：黄海青" Style="{StaticResource DescriptionStyle}"/>
            <TextBlock Text=" QQ ：625415003" Style="{StaticResource DescriptionStyle}"/>
        </StackPanel>
    </Grid>
</controls:BaseWindow>
